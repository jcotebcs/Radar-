# Radar

This repository serves as a placeholder. It originally lacked a terminating
newline, which caused the shell prompt to appear directly after the header
when viewed in a terminal. This commit adds the missing newline and a short
description to clarify the project's purpose.
